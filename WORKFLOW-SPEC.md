# JOY CHIN 實際營運流程（2026-07-19 訪談版）

## 設計原則

- 現場可以先做事、之後補關聯，但系統必須留下醒目的未補資料提醒。
- 庫存不直接改寫歷史；所有進出、盤點及沖銷均留下異動。
- 供應商短交不要求每天追貨，未交量保留為「待下次併單」。
- 供應商混單到貨先快速登記成到貨批次，之後再逐項配對採購品項。
- 每次出貨只對應一位客戶；訂單可暫時未指定，但必須列入待補清單。
- 助理使用少量大型操作入口；主管保留全局、成本、盤點調整與例外放行權限。

## 產品開發與上線驗收準則

以下準則適用於所有新模組、表單、流程與既有功能修改，不只限於庫存頁面。

### 1. 從實際工作流程設計

- 開發前先確認誰在什麼情境操作、手上實際有哪些資料，以及下一步要交給誰。
- 訪談正常流程時必須同時詢問邊緣案例，例如資料不完整、重複建立、部分到貨、臨時出貨、取消、網路失敗及事後補登。
- 第一線人員應能先完成必要現場動作；可後補的關聯資料必須形成明確待辦，不能靜默遺失。
- 主管專用的成本、全局資料、盤點調整及例外放行，不應干擾助理的日常入口。

### 2. 欄位必須讓使用者看得懂

- 欄位名稱應使用現場語言，避免只顯示資料庫或程式術語。
- 容易混淆的欄位必須說明「要填什麼、為什麼需要、會影響哪個結果」。
- 同時存在原始量與換算量時，必須清楚區分「現場實際數量」與「實際入帳／庫存異動數量」。
- 系統已能推導的欄位應自動帶入或隱藏；只有真正的例外才要求人工輸入。
- 人工覆寫重要計算或例外放行時，必須記錄原因與操作者，供日後追溯。

### 3. 選單、搜尋與主檔一致性

- 產品、客戶、供應商、訂單等會持續增長的主檔選單，必須可搜尋，不可只依賴長下拉清單。
- 搜尋至少涵蓋使用者會辨認的名稱、代碼及主要關聯資料；選項應顯示足夠資訊避免選錯。
- 同一實體有多筆聯絡人資料時，公司型選單必須以公司去重；只有需要指定聯絡人時才顯示個人列。
- 去重比較需處理前後空白、連續空白、大小寫及全形／半形差異，但不得因此刪除原始聯絡人或歷史資料。
- 新增客戶、供應商或產品前應提示相似資料，讓使用者確認是否仍要新增。

### 4. 單位與計算不可猜測

- 每筆數量都必須保存原始數量、原始單位、換算後數量、庫存標準單位及換算依據。
- 只有產品具有可靠換算係數時才可自動換算；缺少係數時提示人工填寫並要求留下依據。
- 卷、米、平方米及公斤換算需使用產品本身的卷長、寬幅及重量係數，不得使用未標示來源的共用假設。
- 金額、數量、利潤、關稅及庫存計算必須有獨立測試，並涵蓋小數精度、零值、負值與缺值。

### 5. 預防重複與半完成資料

- 優先用系統設計降低人為記憶負擔，例如記錄下載、發送、重發、確認及最後操作者。
- 儲存按鈕需防止重複點擊；跨表寫入應保持原子性，失敗時不可留下無法辨認的半完成資料。
- 網路失敗或重新整理後，使用者必須能分辨資料尚未送出、已成功或需要重試。
- 歷史錯誤以沖銷、版本或狀態變更修正，不直接刪除已影響營運的紀錄。

### 6. 權限與敏感資料以後端為準

- 不可只靠隱藏按鈕或瀏覽器狀態控制權限；直接呼叫 API 時也必須受到相同限制。
- 每項新功能需驗證未登入、一般使用者、助理、主管及外部角色的讀寫範圍。
- 成本、客戶資料、文件及附件必須由 RLS、Policy 或受控後端流程保護。

### 7. 每次發布的最低驗收

- 桌面與手機實際操作主要流程，至少覆蓋主管與第一線使用者介面。
- 驗證搜尋、空資料、重複資料、錯誤輸入、慢速／失敗回應及重新整理後的一致性。
- 執行自動測試與程式差異檢查；新增的重要規則需補回歸測試。
- 若變更資料表、RLS、Policy 或 Storage，migration 必須先在隔離環境驗證，再套用正式環境。
- 發布時更新版本與文件，部署後從正式網址回讀版本並完成一輪關鍵流程 smoke test。

## 詢價到報價

1. 詢價主要來自 LINE，最低資料是客戶名稱與需求量。
2. 數量或目標價未定時，標記為新產品開發案，不阻擋建立。
3. 未登記客戶可先建立詢價，成交後再轉正式客戶；轉換前檢查相似名稱。
4. 同一需求可同時詢問多個供應商，分別記錄管道、詢問日、承諾回覆日、狀態與附件。
5. 報價採版本制；客戶可只接受部分品項，接受品項才轉訂單。
6. 過期報價可選擇沿用，但必須留下沿用決定與供應商價格未重驗的提醒。

## 採購與到貨

1. 不同供應商必須分成不同採購單；同一供應商可合併多個客戶訂單。
2. 採購單保留下載、發送、重發的時間及操作者，避免重複發送。
3. 可先採購後補客戶訂單，但列入紅色待補關聯清單。
4. 混合到貨先建 `receiving_batches`，無法辨認的品項維持待辨識。
5. 實收少於採購量時，自動建立 `procurement_shortfalls`；預設待下次併單，而非每天催貨。
6. 實收超量必須記錄，並由主管確認是否入庫。
7. 只能做外觀檢查；外觀無損可直接出貨，客戶發現內部品質問題後轉客訴處理。

## 庫存與樣品

1. `inventory_transactions` 是進出存的事實來源，`products.stock_qty` 暫時保留為快取欄位。
2. 客戶出貨必須選客戶，以實際出貨日入帳；訂單可後補。
3. 樣品、免費贈送、公司自用、報廢及退貨均為獨立異動類型。
4. 不良品進隔離狀態，不計可用庫存；錯誤異動以反向沖銷更正，不刪除。
5. 盤點輸入實際量，系統產生盤盈或盤虧異動，不需要等到月底才能盤。
6. 原則上禁止負庫存；僅主管可輸入原因後放行。
7. 支援卷、米、平方米、公斤、箱；未有可靠換算係數時不可假裝自動換算。
8. 未來以批次／每卷實際長度管理不同卷長、公斤與米／平方米換算。

## 尚未自動決定的例外

- 客戶取消但供應商已下單：預設轉一般庫存，並標示「取消訂單轉庫存」供主管檢視。
- 補登日期：允許補登；月結後鎖帳列為下一階段功能，主管可例外放行。
- 客戶退貨：直接進待處理／隔離，不自動回到可用庫存。
